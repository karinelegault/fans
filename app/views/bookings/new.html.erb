

<div class="container text-center">
  <div class="form-card-booking row justify-content-center">
    <div class="col-sm-12">
      <h3>Add a booking start date</h3>
          <%= simple_form_for [@fan, @booking] do |f| %>
            <%= f.input :start_date %>
            <%= f.input :end_date %>
            <%= f.button :submit, class: "btn btn-primary" %>
          <% end %>
    </div>
    <div class="col-sm-4">
      
    </div>
  </div>
</div>

