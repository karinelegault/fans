<div class="container">

  <h1><%= @fan.name %></h1>
  <%= link_to "Back to all fans", root_path %>
  <br>
  <%# We'll do the cloudinary photos aftewards. %>
  <%# <%= cl_image_tag @fan.photo, height: 300, width: 400, crop: :fill %>


  <div class="row">
    <div class="col-sm-12 col-md-6">
      <h3>Fans</h3>
      <table class="table table-bordered">
        <tbody>
          <thead>
            <th>Description</th>
            <th>Price</th>
          </thead>
        </tbody>

      </table>
    </div>
  </div>

</div>
<p id="notice" style="color: green" text-center><%= notice %></p>

<div class="container">
  <h1><%= @fan.name %></h1>
  <%= link_to "Back to all fans", fans_path  %>
  <br>
  <%# We'll do the cloudinary photos aftewards. (paslm)%>
  <%# <%= cl_image_tag @fan.photo, height: 300, width: 400, crop: :fill %>


<div>
<%= @fan.description %>
</div>
